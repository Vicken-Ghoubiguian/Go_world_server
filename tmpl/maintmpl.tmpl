{{define "maintmpl"}}
<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<link rel="stylesheet" href="../styles/stylesheet.css">
		<link rel="icon" href="../styles/favicon.png" type="image/png">
		<title>{{.Title}}</title>
	</head>
	<body>
		{{if true}}

			{{template "bodytmpl" .}}

		{{else}}

			{{template "bodytmpl" .}}
		
		{{end}}

	</body>
</html>
{{end}}
